---
title: Manual OIDC Configuration
description: Reference for manually configuring OIDC clients with MC-ID.
---

import { Code } from "@astrojs/starlight/components";

This guide provides the technical details needed to configure a generic OIDC client or to implement the flow manually.

## Endpoints

If you are self-hosting MC-ID or configuring a client manually, use the following endpoints. Replace `https://mc-id.com` with your instance URL if self-hosting.

| Endpoint          | Path                         | Description                         |
| :---------------- | :--------------------------- | :---------------------------------- |
| **Issuer**        | `https://mc-id.com`          | The OIDC Issuer URL.                |
| **Authorization** | `/dashboard/oauth/authorize` | URL to redirect users to for login. |
| **Token**         | `/api/v1/oauth/token`        | URL to exchange code for tokens.    |
| **Userinfo**      | `/api/v1/oauth/userinfo`     | URL to fetch user claims.           |
| **JWKS**          | `/api/v1/oauth/jwks`         | URL to fetch public signing keys.   |

## Scopes

MC-ID supports the following scopes:

- `openid`: **(Required)** Signals that the request is an OpenID Connect request.
- `profile`: Requests access to the user's default profile claims (name, picture, etc.).
- `email`: Requests access to the user's email address.
- `mcid`: Requests access to Minecraft-specific information (UUID, username).

## User Info Response

When using the `mcid` scope, the ID token and Userinfo endpoint will return additional claims containing the Minecraft profile.

<Code
  code={`{
  "sub": "user_1234567890",
  "name": "Steve",
  "picture": "https://mc-id.com/avatars/steve.png",
  "email": "steve@example.com",
  "minecraft": {
    "uuid": "069a79f444e94726a5befca90e38aaf5",
    "username": "Notch"
  }
}`}
  lang="json"
/>
